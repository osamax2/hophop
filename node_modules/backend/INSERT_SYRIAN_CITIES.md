# إضافة جميع المدن السورية إلى قاعدة البيانات

## نظرة عامة
هذا السكريبت يضيف جميع المدن السورية الرئيسية إلى قاعدة البيانات مع إحداثياتها الجغرافية (خطوط الطول والعرض).

## الطرق المتاحة

### الطريقة 1: استخدام سكريبت Node.js (موصى به)
```bash
cd backend
node insert_syrian_cities.js
```

**المميزات:**
- ✅ يعرض تقرير مفصل عن المدن المضافة
- ✅ يتعامل مع الأخطاء بشكل أفضل
- ✅ يمنع التكرار تلقائياً
- ✅ يعرض إحصائيات نهائية

### الطريقة 2: استخدام ملف SQL مباشرة
```bash
# باستخدام psql
psql -U your_username -d your_database -f insert_syrian_cities.sql

# أو باستخدام pgAdmin
# افتح pgAdmin → افتح Query Tool → انسخ محتوى insert_syrian_cities.sql → نفذ
```

## المدن المضافة

السكريبت يضيف **أكثر من 100 مدينة سورية** تشمل:

### عواصم المحافظات (14 محافظة)
- دمشق (Damascus)
- حلب (Aleppo)
- حمص (Homs)
- اللاذقية (Latakia)
- طرطوس (Tartus)
- دير الزور (Deir ez-Zor)
- الحسكة (Al-Hasakah)
- الرقة (Ar-Raqqah)
- السويداء (As-Suwayda)
- درعا (Daraa)
- إدلب (Idlib)
- حماة (Hama)
- القنيطرة (Quneitra)
- دوما (Douma)

### المدن الرئيسية في كل محافظة
- محافظة دمشق: داريا، السيدة زينب، التل، الزبداني، قطنا، يبرود، النبك، وغيرها
- محافظة حلب: منبج، الباب، عفرين، إعزاز، السفيرة، تل رفعت، وغيرها
- محافظة حمص: تدمر، الرستن، تلكلخ، مصياف، القصير، وغيرها
- محافظة اللاذقية: جبلة، بانياس، صافيتا، الحفة، قرداحة، وغيرها
- محافظة طرطوس: الدريكيش، الشيخ بدر، وغيرها
- محافظة دير الزور: البوكمال، الميادين، أبو كمال
- محافظة الحسكة: القامشلي، رأس العين، المالكية، عامودا، ديريك، تل تمر، وغيرها
- محافظة الرقة: عين العرب، الطبقة
- محافظة السويداء: شهبا، صلخد
- محافظة درعا: بصرى، إزرع، نوى، طفس، الشيخ مسكين، جاسم، وغيرها
- محافظة إدلب: معرة النعمان، جسر الشغور، أريحا، كفر تخاريم، حارم، وغيرها
- محافظة حماة: السلمية، محردة، كفر زيتا، اللطامنة، مورك، وغيرها

## معلومات الإحداثيات

كل مدينة تحتوي على:
- **الاسم** (name): اسم المدينة بالعربية
- **رمز الدولة** (country_code): 'SY' لسوريا
- **خط العرض** (latitude): الإحداثي الشمالي/الجنوبي
- **خط الطول** (longitude): الإحداثي الشرقي/الغربي

## ملاحظات مهمة

1. **منع التكرار**: السكريبت يستخدم `ON CONFLICT DO NOTHING` لمنع إضافة مدن موجودة مسبقاً
2. **الإحداثيات تقريبية**: الإحداثيات المضافة هي تقريبية. للحصول على إحداثيات دقيقة أكثر، يمكن استخدام خدمات Geocoding
3. **إضافة مدن جديدة**: يمكنك بسهولة إضافة مدن جديدة إلى المصفوفة في `insert_syrian_cities.js`

## التحقق من النتائج

بعد تنفيذ السكريبت، يمكنك التحقق من المدن المضافة:

```sql
-- عرض جميع المدن
SELECT * FROM cities ORDER BY name;

-- عدد المدن
SELECT COUNT(*) FROM cities;

-- البحث عن مدينة معينة
SELECT * FROM cities WHERE name LIKE '%دمشق%';
```

## استكشاف الأخطاء

إذا واجهت مشاكل:

1. **تأكد من اتصال قاعدة البيانات**: تحقق من ملف `.env` في مجلد `backend`
2. **تحقق من وجود جدول cities**: تأكد من أن الجدول موجود في قاعدة البيانات
3. **تحقق من الأعمدة**: تأكد من وجود الأعمدة التالية:
   - `name` (VARCHAR)
   - `country_code` (VARCHAR)
   - `latitude` (NUMERIC/DECIMAL)
   - `longitude` (NUMERIC/DECIMAL)

## مثال على المخرجات

عند تنفيذ السكريبت بنجاح، سترى:

```
🌍 Inserting Syrian cities into database...

✅ Inserted: دمشق
✅ Inserted: حلب
⏭️  Skipped (already exists): حمص
...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Summary:
   ✅ Inserted: 95 cities
   ⏭️  Skipped (already exist): 5 cities
   ❌ Errors: 0 cities
   📍 Total processed: 100 cities
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📈 Total cities in database: 100
```
