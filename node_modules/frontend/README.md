
  # Responsive Bus Schedule Platform

  This is a code bundle for Responsive Bus Schedule Platform.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  