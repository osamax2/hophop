# PostgreSQL with init script
FROM postgres:16-alpine

# Copy init scripts (schema and data)
COPY docker-init/*.sql /docker-entrypoint-initdb.d/

# Set permissions
RUN chmod 644 /docker-entrypoint-initdb.d/*.sql
