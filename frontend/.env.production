# Production API Configuration
# Empty string means relative paths will be used (recommended for same-domain deployment)
VITE_API_BASE="http://localhost:4000"

# Alternative: Use full domain when ready
# VITE_API_BASE="http://hophopsy.com"
