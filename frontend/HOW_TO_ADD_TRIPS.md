# كيفية إضافة رحلات كشركة أو أدمن
## Responsive Bus Schedule Platform

---

## 📋 الطرق المتاحة لإضافة الرحلات

هناك **3 طرق** لإضافة رحلات:

1. **من لوحة الإدارة (واجهة المستخدم)** - الأسهل
2. **من API مباشرة** - للمطورين
3. **استيراد CSV** - للإضافة الجماعية

---

## 1. إضافة رحلة من لوحة الإدارة (واجهة المستخدم)

### الخطوات:

1. **سجل دخول بحساب Admin أو Agent:**
   - افتح التطبيق: http://localhost:5173
   - سجل دخول بحساب لديه صلاحيات Admin أو Agent

2. **اذهب إلى لوحة الإدارة:**
   - اضغط على "لوحة الإدارة" في القائمة
   - أو افتح: http://localhost:5173 (بعد تسجيل الدخول)

3. **افتح تبويب إدارة الجداول:**
   - اضغط على تبويب "إدارة الجداول" أو "Schedule Management"

4. **اضغط على زر "إضافة جدول":**
   - زر أخضر في أعلى الصفحة

5. **املأ البيانات المطلوبة:**

   **الحقول الإلزامية:**
   - ✅ **المسار (Route):** اختر من القائمة المنسدلة
   - ✅ **الشركة (Company):** اختر الشركة
   - ✅ **نوع النقل (Transport Type):** عادي / VIP / فان
   - ✅ **محطة المغادرة:** اختر المحطة
   - ✅ **محطة الوصول:** اختر المحطة
   - ✅ **وقت المغادرة:** اختر التاريخ والوقت
   - ✅ **وقت الوصول:** اختر التاريخ والوقت
   - ✅ **عدد المقاعد:** أدخل العدد (مثل: 50)

   **الحقول الاختيارية:**
   - ⚠️ **المدة (بالدقائق):** سيتم حسابها تلقائياً إن لم تدخلها
   - ⚠️ **المرافق (Equipment):** مثل: wifi,ac,usb
   - ⚠️ **سياسة الإلغاء:** نص توضيحي
   - ⚠️ **معلومات إضافية:** أي معلومات أخرى

6. **اضغط "إضافة":**
   - سيتم إضافة الرحلة وإغلاق النافذة
   - ستظهر رسالة نجاح

---

## 2. إضافة رحلة من API مباشرة

### باستخدام curl:

```bash
curl -X POST "http://localhost:4000/api/admin/trips" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "route_id": 1,
    "company_id": 1,
    "transport_type_id": 1,
    "departure_station_id": 1,
    "arrival_station_id": 2,
    "departure_time": "2024-12-20T08:00:00",
    "arrival_time": "2024-12-20T12:00:00",
    "duration_minutes": 240,
    "seats_total": 50,
    "equipment": "wifi,ac,usb",
    "cancellation_policy": "Free cancellation up to 24h",
    "extra_info": "Additional information"
  }'
```

### الحقول المطلوبة:

| الحقل | النوع | الوصف |
|-------|-------|-------|
| `route_id` | number | ID المسار |
| `company_id` | number | ID الشركة |
| `transport_type_id` | number | ID نوع النقل |
| `departure_station_id` | number | ID محطة المغادرة |
| `arrival_station_id` | number | ID محطة الوصول |
| `departure_time` | string | وقت المغادرة (ISO format) |
| `arrival_time` | string | وقت الوصول (ISO format) |
| `duration_minutes` | number | المدة بالدقائق |
| `seats_total` | number | إجمالي المقاعد |

### الحقول الاختيارية:

| الحقل | النوع | الوصف |
|-------|-------|-------|
| `status` | string | الحالة (default: "scheduled") |
| `is_active` | boolean | نشط (default: true) |
| `equipment` | string | المرافق |
| `cancellation_policy` | string | سياسة الإلغاء |
| `extra_info` | string | معلومات إضافية |

---

## 3. استيراد رحلات من CSV

### الخطوات:

1. **اذهب إلى لوحة الإدارة**
2. **افتح تبويب "استيراد البيانات"**
3. **اختر ملف CSV:**
   - اضغط "اختر ملف"
   - اختر ملف CSV

4. **معاينة البيانات:**
   - اضغط "معاينة" لرؤية البيانات قبل الاستيراد
   - تحقق من صحة البيانات

5. **استيراد:**
   - اضغط "استيراد" لإضافة الرحلات

### صيغة ملف CSV:

```csv
route_id,company_id,transport_type_id,departure_station_id,arrival_station_id,departure_time,arrival_time,duration_minutes,seats_total
1,1,1,1,2,2024-12-20 08:00:00,2024-12-20 12:00:00,240,50
1,2,2,1,2,2024-12-20 10:00:00,2024-12-20 14:00:00,240,40
```

**الأعمدة المطلوبة:**
- `route_id`
- `company_id`
- `departure_time`
- `arrival_time`
- `seats_total`

**الأعمدة الاختيارية:**
- `transport_type_id` (default: 1)
- `departure_station_id`
- `arrival_station_id`
- `duration_minutes`

---

## 📝 الحصول على IDs المطلوبة

### 1. الحصول على Routes:

```bash
curl "http://localhost:4000/api/admin/routes" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 2. الحصول على Companies:

```bash
curl "http://localhost:4000/api/companies"
```

### 3. الحصول على Transport Types:

```bash
curl "http://localhost:4000/api/transport-types"
```

### 4. الحصول على Stations:

```bash
curl "http://localhost:4000/api/stations"
```

### 5. الحصول على Cities:

```bash
curl "http://localhost:4000/api/cities"
```

---

## ✅ مثال كامل

### مثال: إضافة رحلة من دمشق إلى حلب

**1. احصل على البيانات المطلوبة:**

```bash
# Routes
curl "http://localhost:4000/api/admin/routes"
# ابحث عن route من دمشق إلى حلب، لاحظ الـ ID (مثلاً: 1)

# Companies
curl "http://localhost:4000/api/companies"
# ابحث عن الشركة، لاحظ الـ ID (مثلاً: 1)

# Transport Types
curl "http://localhost:4000/api/transport-types"
# ابحث عن النوع، لاحظ الـ ID (مثلاً: 1 = عادي)

# Stations
curl "http://localhost:4000/api/stations"
# ابحث عن محطة المغادرة والوصول، لاحظ الـ IDs
```

**2. أضف الرحلة:**

```bash
curl -X POST "http://localhost:4000/api/admin/trips" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "route_id": 1,
    "company_id": 1,
    "transport_type_id": 1,
    "departure_station_id": 1,
    "arrival_station_id": 2,
    "departure_time": "2024-12-20T08:00:00",
    "arrival_time": "2024-12-20T12:00:00",
    "duration_minutes": 240,
    "seats_total": 50,
    "equipment": "wifi,ac",
    "cancellation_policy": "إلغاء مجاني حتى 24 ساعة قبل الرحلة"
  }'
```

---

## 🔧 ملاحظات مهمة

1. **للشركات (Agent):**
   - يمكنك فقط إضافة رحلات لشركتك
   - تأكد من استخدام `company_id` الخاص بشركتك

2. **للأدمن:**
   - يمكنك إضافة رحلات لأي شركة
   - لديك صلاحيات كاملة

3. **التحقق من البيانات:**
   - تأكد من وجود Route قبل الإضافة
   - تأكد من وجود Stations قبل الإضافة
   - تأكد من صحة التواريخ والأوقات

4. **حساب المدة تلقائياً:**
   - إذا لم تدخل `duration_minutes`، سيتم حسابها تلقائياً من `departure_time` و `arrival_time`

---

## 🐛 حل المشاكل

### المشكلة: خطأ "Missing required fields"

**الحل:** تأكد من إدخال جميع الحقول الإلزامية:
- route_id
- company_id
- transport_type_id
- departure_station_id
- arrival_station_id
- departure_time
- arrival_time
- seats_total

### المشكلة: خطأ "Route not found"

**الحل:** تأكد من وجود Route في قاعدة البيانات:
```sql
SELECT * FROM routes;
```

### المشكلة: خطأ "Station not found"

**الحل:** تأكد من وجود Stations:
```sql
SELECT * FROM stations;
```

---

## 📚 المزيد من المعلومات

- **دليل الاختبار:** `TESTING_GUIDE.md`
- **توثيق API:** `backend/API_DOCUMENTATION.md`

---

**جاهز لإضافة الرحلات!** 🚌
